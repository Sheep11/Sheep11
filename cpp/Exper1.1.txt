Stack:主要包含一个链表节点对象的头指针，通过头指针进行元素的增加与删除

Difference：
	Stash：类型中包含一个char*变量用来存储数据，数组连续的存储空间可以对数据进行随机的访问。
但是这样不可避免的会浪费一些空间，因为不可能每次都申请空间，只能是预留出一些来备用。
这也会导致当空间不足需要重新分配时，新申请的空间与原空间没有任何交集，需要将原空间中的内容再拷贝至新的空间中。

	Stack:用链表来存储数据，意味着数据不能够被随机的访问。
但是当分配空间会更加的灵活：需要时只需新申请一个link而不影响之前的空间与数据，也意味着对于空间完全的利用。